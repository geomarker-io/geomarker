# geomarker 0.1.0.9000

- Now imports `stow (>= 0.3.0)` to manage function-owned durable local copies.

- Renamed `get_evi_data()` to `get_evi_summary()` and
  `get_nlcd_fct_imp_data()` to `get_nlcd_fct_imp_summary()`.
