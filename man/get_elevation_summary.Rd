% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elevation.R
\name{get_elevation_summary}
\alias{get_elevation_summary}
\title{Summarize nearby elevation}
\usage{
get_elevation_summary(x, fun = stats::median, buffer = 800, ...)
}
\arguments{
\item{x}{a s2_cell_dates vector (see \code{?s2cd})}

\item{fun}{function to summarize extracted values}

\item{buffer}{distance from s2 cell (in meters) to summarize data}

\item{...}{passed to \code{\link[stow:stow]{stow::stow()}}. The \code{package} and \code{subdir} arguments
are fixed by geomarker, and \code{etag} is always set to \code{FALSE} for this
versioned release asset.}
}
\value{
numeric vector of elevation summaries
}
\description{
Summarizes elevations within
the buffer distance of each s2 cell;
dates are not used to link to elevation data.
}
\details{
Elevation data come from a reproducible snapshot of the U.S. Geological
Survey 3D Elevation Program (3DEP) Bare Earth DEM Dynamic service. The
service snapshot, reflecting 3DEP data published through July 20, 2026, was
exported directly as a single-band signed 16-bit GeoTIFF in EPSG:5070 at an
800 m grid and is distributed as a versioned geomarker release asset.
See \url{https://www.usgs.gov/3d-elevation-program/about-3dep-products-services}
and
\url{https://elevation.nationalmap.gov/arcgis/rest/services/3DEPElevation/ImageServer}.
Data courtesy of the U.S. Geological Survey.
}
\examples{
 withr::local_envvar(
   R_USER_DATA_DIR = fs::path_package(
     "geomarker",
     "gmrkr--8841"
   ),
   R_GEOMARKER_NO_DOWNLOAD = "true"
 )
get_elevation_summary(s2cd_example())
get_elevation_summary(s2cd_example(), stats::sd, 1400)
}
