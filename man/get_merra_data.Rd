% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merra.R
\name{get_merra_data}
\alias{get_merra_data}
\alias{install_merra_data}
\title{MERRA-2 aerosol diagnostics data}
\usage{
get_merra_data(x, ...)

install_merra_data(
  merra_month,
  source = c("release", "earthdata"),
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{x}{a s2_cell_dates vector (see \code{?s2cd})}

\item{...}{passed to \code{geomarker_download_file()} when an official release
asset is needed}

\item{merra_month}{character vector of calendar months in \code{YYYY-MM} format,
from 2017 onward}

\item{source}{either \code{"release"} to install the matching official half-year
asset or \code{"earthdata"} to build each complete month from NASA}

\item{overwrite}{logical; recreate monthly data from the latest CMR listing?
Matching daily source caches are still reused.}

\item{quiet}{logical; suppress progress messages?}
}
\value{
A named list with one data frame per input location and one row per
requested date. Columns are \code{merra_dust}, \code{merra_oc}, \code{merra_bc},
\code{merra_ss}, \code{merra_so4}, and \code{merra_pm25}. Unavailable periods contain
missing values and produce one warning.

\code{install_merra_data()} returns a named character vector of installed
paths. Months in the same released half-year may have the same path.
}
\description{
Links daily surface PM2.5 and its aerosol components from NASA's MERRA-2
\code{M2T1NXAER} v5.12.4 product to each location and date in an \code{s2cd} vector.
}
\details{
Each package release records one complete January--June and one complete
July--December asset per available year. A fully available calendar month
may instead be built locally with \code{install_merra_data(source = "earthdata")}. Local monthly data take precedence over a released half-year
asset.

Source builds require a free NASA Earthdata Login account. Follow NASA's
\href{https://urs.earthdata.nasa.gov/documentation/for_users/how_to_register}{registration guide}
to create and activate an account. Before the first source build, sign in to
the \href{https://opendap.earthdata.nasa.gov/login}{Cloud OPeNDAP service} and
authorize the \verb{Hyrax in the cloud} application when prompted. NASA also
documents how to
\href{https://urs.earthdata.nasa.gov/documentation/for_users/how_to_preauth_app}{preauthorize an application}
if the prompt does not appear.

Set the account credentials in \code{EARTHDATA_USER} and \code{EARTHDATA_PASSWORD}.
They are written to temporary private netrc and cookie files in the
geomarker data cache while NASA's authenticated redirects are followed.

Source concentrations are converted to micrograms per cubic meter and
averaged across each day. Total PM2.5 is calculated as \code{DUSMASS25 + OCSMASS + BCSMASS + SSSMASS25 + SO4SMASS * 132.14 / 96.06}.
}
\examples{
withr::local_envvar(
  R_USER_DATA_DIR = fs::path_package("geomarker", "gmrkr--8841"),
  R_GEOMARKER_NO_DOWNLOAD = "true"
)
get_merra_data(s2cd_example())
}
\references{
\url{https://disc.gsfc.nasa.gov/datasets/M2T1NXAER_5.12.4/summary}

\url{https://gmao.gsfc.nasa.gov/gmao-products/merra-2/faq_merra-2/}
}
