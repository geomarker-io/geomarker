% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nei.R
\name{get_nei_point_summary}
\alias{get_nei_point_summary}
\title{Summarize nearby National Emissions Inventory point emissions}
\usage{
get_nei_point_summary(
  x,
  pollutant_code = c("PM25-PRI", "EC", "OC", "SO4", "NO3", "PMFINE"),
  fun = sum,
  buffer = 1000,
  ...
)
}
\arguments{
\item{x}{a s2_cell_dates vector (see \code{?s2cd})}

\item{pollutant_code}{one of \code{PM25-PRI}, \code{EC}, \code{OC}, \code{SO4}, \code{NO3}, or
\code{PMFINE}}

\item{fun}{function used to summarize nearby annual emissions}

\item{buffer}{distance from s2 cell (in meters) to summarize data}

\item{...}{passed to \code{geomarker_download_file()}}
}
\value{
a list of numeric vectors of point-source emissions summaries, one
vector per input location and one value per associated date. Values passed
to \code{fun} are in tons per year. Dates mapping to unavailable inventory
cycles return \code{NA_real_}.
}
\description{
Summarizes annual facility-level point emissions within \code{buffer} meters of
each s2 cell as tons per year. Each date is mapped by
calendar year to the nearest scheduled triennial National Emissions
Inventory (NEI) cycle
(2016-2018 to 2017 NEI, 2019-2021 to 2020 NEI, 2022-2024 to 2023 NEI).
Dates mapping to an unavailable or
unsupported cycle, including 2025 through 2027 mapping to the not-yet-
available 2026 NEI, return \code{NA_real_}.
}
\details{
Facility-summary ZIP files for each requested pollutant are downloaded
from the Environmental Protection Agency and read directly on each call.
Rows are retained only when the pollutant code, latitude, longitude, and
total emissions are complete. Latitude, longitude, and total emissions must
also be finite, with latitude between -90 and 90 degrees and longitude
between -180 and 180 degrees.

\code{fun} receives the numeric vector of annual emissions from facilities within
the buffer and must return one numeric value. With the default \code{sum}, a
location with no nearby facilities returns zero.
}
\examples{
withr::local_envvar(
  R_USER_DATA_DIR = fs::path_package(
    "geomarker",
    "gmrkr--8841"
  ),
  R_GEOMARKER_NO_DOWNLOAD = "true"
)
get_nei_point_summary(s2cd_example())
get_nei_point_summary(s2cd_example(), "PMFINE", buffer = 1600)
get_nei_point_summary(s2cd_example_cincy(5L), "PMFINE", buffer = 10000)
}
\references{
\url{https://www.epa.gov/air-emissions-inventories/national-emissions-inventory-nei}

\url{https://www.epa.gov/air-emissions-inventories/what-are-units-nei-emissions-data}
}
